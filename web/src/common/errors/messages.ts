import { defineMessages } from 'react-intl';

const messages = defineMessages({
  invalidJSONPayload: {
    id: 'COMMON.API.INVALID_JSON_ERROR',
    defaultMessage: 'Invalid payload provided',
    description: 'User provided invalid api payload',
  },
});

export default messages;
